<%= form_for(@feedbackcomment) do |f| %>
  <% if @feedbackcomment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@feedbackcomment.errors.count, "error") %> prohibited this feedbackcomment from being saved:</h2>

      <ul>
      <% @feedbackcomment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br>
    <%= f.text_field :user %>
  </div>

  <!--
  <div class="field">
    <%= f.label :created %><br>
    <%= f.datetime_select :created %>
  </div>
  <div class="field">
    <%= f.label :updated %><br>
    <%= f.datetime_select :updated %>
  </div>
  -->
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit :class => "btn btn-primary" %>
  </div>
<% end %>
